#include "map.h"
/*
Map::Map(int width, int height){
    this->width = width;
    this->height = height;
    this->matrix = new Tile*[width];
    for(int i = 0; i < width; i++){
        this->matrix[i] = new Tile[height];
    }
}

Map::~Map(){
    for(int i = 0; i < this->width; i++){
        delete[] this->matrix[i];
    }
    delete[] this->matrix;
}

void add_game_object(int x, int y, int object){
    this->matrix[x][y].add_object(object);
}*/

